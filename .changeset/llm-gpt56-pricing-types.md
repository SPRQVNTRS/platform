---
"@sprqvntrs/llm": minor
---

feat(llm): add the gpt-5.6 family (terra/luna/sol plus their pro variants) to `OpenAINewerModel` and to pricing under both bare and `openai/`-prefixed keys, and refresh `pricing-data.json` from OpenRouter. gpt-5.4-mini pricing is regression-tested (fixes #20; #12)
