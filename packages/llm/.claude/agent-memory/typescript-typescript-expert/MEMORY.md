# Memory Index

## Project
- [llm pkg verification commands](project_llm_pkg_verification_commands.md) — no `typecheck` script; `pnpm test` hits live paid APIs; vitest `include` is an allow-list that silently skips new files
- [OpenRouter reasoning effort / dual request paths](project_llm_openrouter_reasoning_effort.md) — chatRequest is built in two places (streaming default + non-streaming); one-path fixes are prod no-ops
